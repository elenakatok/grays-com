rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Locked down until real rules are implemented
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
